---
upgrade:
  - |
    ``etcd`` has been updated from ``3.6.5`` to ``3.6.14``.
  - |
    ``letsencrypt-lego`` has been updated from ``4.27.0`` to ``5.4.0``.
  - |
    ``magnum-conductor-plugin-helm`` has been updated from ``3.19.0`` to
    ``4.2.4``.
  - |
    ``prometheus-alertmanager`` has been updated from ``0.31.1`` to ``0.34.0``.
  - |
    ``prometheus-cadvisor`` has been updated from ``0.56.2`` to ``0.60.5``.
  - |
    ``prometheus-elasticsearch-exporter`` has been updated from ``1.10.0`` to
    ``1.11.0``.
  - |
    ``prometheus-libvirt-exporter`` has been updated from ``2.3.1`` to
    ``2.5.0``.
  - |
    ``prometheus-memcached-exporter`` has been updated from ``0.15.5`` to
    ``0.17.0``.
  - |
    ``prometheus-mtail`` has been updated from ``3.4.0`` to ``3.4.9``.
  - |
    ``prometheus-mysqld-exporter`` has been updated from ``0.18.0`` to
    ``0.20.0``.
  - |
    ``prometheus-node-exporter`` has been updated from ``1.10.2`` to
    ``1.12.1``.
  - |
    ``prometheus-server`` has been updated from ``3.5.4`` to ``3.5.5``.
  - |
    ``prometheus-valkey-exporter`` has been updated from ``1.82.0`` to
    ``1.89.0``.
