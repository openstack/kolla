---
- name: COPR Erlang update
  hosts: all
  tasks:
    - name: Trigger COPR builds via webhooks
      ansible.builtin.uri:
        url: "{{ item | trim }}"
        method: POST
      loop: "{{ kolla_copr_webhooks.urls }}"
